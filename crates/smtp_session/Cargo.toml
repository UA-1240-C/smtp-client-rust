[package]
name = "smtp_session"
version = "0.0.0"
edition = "2021"

[dependencies]
async_stream = { path = "../async_stream" }
error_handler = { path = "../error_handler" }
tokio = { version = "1", features = ["full"] }
base64 = "0.22.1"
regex = "1.4"